library analysis.src.canonicalizer;

import 'package:analyzer/analyzer.dart';

/// A utility class that creates a unique reference to every type.
class Canonicalizer {
  int getLibraryId(Uri source, Declaration declaration) {
    // declaration.element
  }
}
